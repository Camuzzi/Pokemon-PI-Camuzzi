const { Router } = require('express');
//const { getAllGenresHandler } = require("../handlers/genresHandler");

const typeRouter = Router();

//typeRouter.get("/",getAllGenresHandler)

module.exports = typeRouter;
